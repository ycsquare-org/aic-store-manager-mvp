{
  "name": "aic-store-manager-mvp",
  "version": "1.0.0",
  "private": true,
  "description": "Alibaba.com Store Manager MVP",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check app.js && node --check image-studio.js && node scripts/validate.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
